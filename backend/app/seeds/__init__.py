"""Curated, deterministic demo-data fixtures for InfraGuard AI.

This package holds the *data* (``assets`` / ``incidents``) and the *builder*
(``runner``) for the official demo seed. It is **development tooling**, not part
of the request path and not a substitute for test fixtures (``tests/``).

Everything here is strictly additive and idempotent - see
``app.seeds.runner`` and ``docs`` for the guarantees.
"""
